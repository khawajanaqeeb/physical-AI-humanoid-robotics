# Environment variables for Physical AI & Humanoid Robotics Educational Platform

# Frontend: Backend API URL (build-time variable for Docusaurus/Vercel)
# This variable is injected at build time and used by the frontend to connect to the backend
# Must be prefixed with NEXT_PUBLIC_ to be available in the browser
# For local development: uses fallback to http://localhost:8000 in auth-client.ts
# For production deployment on Vercel:
#   1. Set this in Vercel Dashboard → Project Settings → Environment Variables
#   2. Variable name: NEXT_PUBLIC_API_URL
#   3. Value: Your Railway backend URL (e.g., https://your-app.up.railway.app)
#   4. Apply to: Production, Preview, Development environments
NEXT_PUBLIC_API_URL=

# Legacy variable (kept for reference, not used by frontend)
BACKEND_URL=http://localhost:8000