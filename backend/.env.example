# =============================================================================
# RAG Chatbot Environment Configuration
# =============================================================================
# This file serves as a template. Copy to .env and fill in your actual values.
# NEVER commit .env to version control!

# Cohere API Configuration (https://dashboard.cohere.com/)
# Get your API key from: https://dashboard.cohere.com/api-keys
COHERE_API_KEY=your-cohere-api-key-here

# Qdrant Cloud Configuration (https://qdrant.tech/)
# Create a cluster and get credentials from: https://cloud.qdrant.io/
QDRANT_URL=https://your-cluster.qdrant.io
QDRANT_API_KEY=your-qdrant-api-key-here
QDRANT_COLLECTION_NAME=textbook_chunks

# Textbook Configuration
# Sitemap URL for content ingestion
TEXTBOOK_SITEMAP_URL=https://physical-ai-humanoid-robotics-e3c7.vercel.app/sitemap.xml

# CORS Configuration
# Comma-separated list of allowed origins (include your deployed Vercel frontend)
CORS_ORIGINS=https://physical-ai-humanoid-robotics-e3c7.vercel.app,http://localhost:3000

# API Security (Optional - leave empty for development)
# Used to protect /api/v1/ingest endpoint
# Generate a secure key: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=

# =============================================================================
# Authentication & User Management Configuration
# =============================================================================

# PostgreSQL Database (Neon - https://neon.tech/)
# Create a database on Neon and paste the connection string here
# Format: postgresql+psycopg://username:password@hostname:5432/database_name (psycopg3 driver)
DATABASE_URL=postgresql+psycopg://user:password@hostname:5432/database

# Better Auth Secret (JWT signing key)
# Generate a secure 64-character hex string: python -c "import secrets; print(secrets.token_hex(32))"
# CRITICAL: Never commit this to version control!
BETTER_AUTH_SECRET=your_64_character_hex_secret_here

# JWT Token Expiration Settings
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7
